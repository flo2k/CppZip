#Programming language
language: cpp

# Compilers
compiler:
    - gcc

addons:
  apt:
    sources:
      - boost-latest
    packages:
      - build-essential
      - libboost1.55-all-dev
      - zlib1g
      - zlib1g-dbg
      - zlib1g-dev
      - doxygen
      - cmake

before_script:
    - mkdir build
    - cmake -H. -Bbuild

# Build steps
script:
  - cmake --build build -- -j
